default {
  dest_storage_account = {
    connection_str = ${?LOCAL_CONNECTION_STR}
    container = "landing"
  }
  project = "GxS_dem"
  token = ${?DX_API_TOKEN}
  stage = "landing"
}

ray =  ${default} {
  dest_storage_account {
    connection_str = ${?RAY_CONNECTION_STR}
  }
}

spark = ${default}

local = ${default}